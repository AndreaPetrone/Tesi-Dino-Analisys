# ANALISI DI VEGETAZIONE - FORME BIOLOGICHE - RAUNKIAER

# Partiamo dal file matrice_specie_plot_Raunkiaer.csv, che contiene:
  Le specie come righe
  Le forme biologiche associate
  Le abbondanze nei plot, già in formato numerico (Braun-Blanquet convertito)

  Perfetto! Hai caricato il file `matrice_specie_plot_Raunkiaer.csv`, che contiene:

* Le **specie** come righe
* Le **forme biologiche** associate
* Le **abbondanze nei plot**, già in formato numerico (Braun-Blanquet convertito)

Possiamo quindi **caricarlo in R** e **procedere direttamente con l’analisi Raunkiaer**!

# 1-CARICAMENTO DEL FILE IN R

# Se il file è nella cartella di lavoro corrente:
dati_raunkiaer <- read.csv("matrice_specie_plot_Raunkiaer.csv", sep = ";", header = TRUE)

# Controlla la struttura
str(dati_raunkiaer)
head(dati_raunkiaer[, 1:5])

# verifica che:
  La **prima colonna** sia "specie"
  La **seconda colonna** sia "forme\_biologiche"
  Le successive colonne siano i **plot** (S\_1, R\_1, S\_1.1, ecc.)

# 2- CREAZIONE DEGLI OGGETTI CHIAVE

# nomi delle specie
rownames(dati_raunkiaer) <- dati_raunkiaer$specie

# Vettore delle forme biologiche
forme_biologiche <- dati_raunkiaer$forme_biologiche

# Matrice con soli valori di abbondanza
matrice_abbondanze <- dati_raunkiaer[, -(1:2)]



